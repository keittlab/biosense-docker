# Server IP to check connectivity
SERVER_IP="10.123.0.1"

# SSH port to check
SSH_PORT=22

# Number of failures before triggering recovery
FAIL_THRESHOLD=24

# Interval in seconds between checks
CHECK_INTERVAL=3600

# Log file location
LOG_FILE="/var/log/test_connectivity.log"
